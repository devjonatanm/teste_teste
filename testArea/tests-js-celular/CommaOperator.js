
const getA = () => 1;
const getB = () => 2;
const getC = () => 3;
const result = (1,2,3,4); //Comma operator (,)
// quando várias variáveis são atribuídas numa const acontece de somente a última ser atribuída, VÍRGULA é um operador, somente com objetos e seus derivados é possível atribuir várias variáveis dentro de uma const

console.log(getA())
console.log(getB())
console.log(getC())
console.log(result)

