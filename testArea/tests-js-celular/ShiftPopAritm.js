const a = [1, 2, 3, 4, 50];
const result = a.shift() - a.pop();
// pega último valor e faz uma operação matemática
// no caso mesmo subtrair ultimo pelo primeiro

console.log(result)


// verifica se há o valor > array.includes(4);
